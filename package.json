{
  "name": "azemoh",
  "description": "Joshua Azemoh's personal website",
  "version": "4.0.0",
  "private": true,
  "main": "gulpfile.js",
  "scripts": {
    "start": "gulp",
    "build": "gulp build",
    "git-commit": "git add -A . && git commit -m \"site update\"",
    "git-push": "git push origin master",
    "deploy": "npm run git-commit && npm run git-push"
  },
  "devDependencies": {
    "browser-sync": "^2.27.10",
    "gulp": "^4.0.2",
    "gulp-autoprefixer": "^8.0.0",
    "gulp-sass": "^5.1.0",
    "gulp-uglifyjs": "^0.6.2",
    "node-sass": "^7.0.1"
  },
  "dependencies": {
    "bourbon": "^7.2.0"
  }
}
